/*
演習4-13 性別 季節 曜日を表す列挙体を自由に定義せよ

作成日 2017年5月7日

作成者 平澤敬介
*/

#include<iostream>
#include<ctime>

using namespace std;

int main()
{
	enum weekly {MON,TUE,WED,THU,FRI,SAT,SUN}; // 曜日の列挙体 日曜日から土曜日に順で定義します

	time_t current = time(NULL);

	struct tm* timer = localtime(&current);

	cout << "西暦" << timer -> tm_year + 1900 << "年" << timer -> tm_mon +1 << "月"
				   << timer -> tm_mday << "日 " << "(" << static_cast<weekly>(timer -> tm_mday-1) << ")";

	return 0;
}
